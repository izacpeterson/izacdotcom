/** @format */
function addProject(name, img) {
  $("#projectList").append(` 
<h1>test</h1>
  `);
  console.log($(`#projectList`));
}
addProject("Homepage", ".");

let projects = [];
projects.push({ name: "proj 1", img: "." });
